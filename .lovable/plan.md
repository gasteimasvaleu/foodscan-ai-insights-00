Diminuir a altura do card de loja em `src/pages/mercado-facil/Index.tsx` — trocar `h-32` por `h-24` no `<Link>` (linha 239). Os elementos internos já se ajustam (`flex flex-col justify-center`), nada mais muda.