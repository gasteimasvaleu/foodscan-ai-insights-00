Trocar o padding superior do container em `src/pages/NutricionistaQueVende.tsx` de `pt-[calc(env(safe-area-inset-top)+2.5rem)]` para `pt-[calc(env(safe-area-inset-top)+4rem)]`, que é o padrão definido no Core memory ("Internal pages need pt-[calc(env(safe-area-inset-top)+4rem)]") e usado pela maioria das páginas internas (About, AdicionarRefeicao, Loja, Quiz, IntermittentFasting). Nenhuma outra alteração.