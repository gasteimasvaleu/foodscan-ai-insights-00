Trocar `pb-28` por `pb-40` no `<main>` de `src/pages/mercado-facil/Index.tsx` (linha 98) para garantir espaço sob a tubelight navbar e o último card de produto não ficar coberto.

Mudança única, sem outros efeitos.