Ajustar os pills do Checklist do dia em `src/pages/Desafio14Dias.tsx` (linhas 641-660):

1. Trocar `flex flex-wrap gap-2` por `grid grid-cols-2 gap-2` para forçar exatamente 2 botões por linha.
2. No botão (linha 649), trocar `rounded-full` por `rounded-xl` (border radius menor).
3. Adicionar `w-full justify-center` para que cada botão preencha sua coluna.